# code-docker-ollama
ollama fork

This repository builds and publishes a Docker image based on the official
[`ollama/ollama`](https://hub.docker.com/r/ollama/ollama) image to GitHub
Container Registry (GHCR).
